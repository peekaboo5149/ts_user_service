services:
  database:
    image: mongo:8
    container_name: ts_user_service_db
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: pass
    volumes:
      - mongodb:/data/db

volumes:
  mongodb: