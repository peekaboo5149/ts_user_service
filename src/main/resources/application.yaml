spring:
  application:
    name: ts_user_service
  web:
    resources:
      add-mappings: false
  mongodb:
    uri: ${TS_USER_SERVICE_MONGODB_URI}
  data:
    mongodb:
      auto-index-creation: true